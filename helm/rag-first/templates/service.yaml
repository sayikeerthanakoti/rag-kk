apiVersion: v1
kind: Service
metadata:
  name: {{ include "rag-first.fullname" . }}
  labels:
    {{- include "rag-first.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rag-first.selectorLabels" . | nindent 4 }}
