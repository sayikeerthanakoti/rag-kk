apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rag-first.fullname" . }}-test-connection"
  labels:
    {{- include "rag-first.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rag-first.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
